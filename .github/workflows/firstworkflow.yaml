# name: Build and push Docker images

# on:
#   push:
#     branches: [ main ]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: putting code on runner
#         run: git clone https://github.com/satywanbhalekar/Github-Actions.git
#         run: cd Github-Actions && docker build -t mynewimage:latest .
#       - name: login to docker hub
#         run: docker login -u satywan -p ${{ secrets.DOCKER_HUB_PASSWORD }}
#       - name:  cd into code and run docker build
#       - name: tag and push the image
#         run: docker tag mynewimage:latest satywan/mynewimage:v1 && docker push satywan/mynewimage:v1

name: Build and push Docker images

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: login to docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
      - name: build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true  
          tags: satywan/mynewimage:v1



      





